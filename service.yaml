---
apiVersion: v1
kind: Service
metadata:
  name: nodeapp-service
spec:
  selector:
    app: nodeapp
  ports:
    - protocol: TCP
      port: 4000
      nodePort: 30001
  type: NodePort # Use NodePort service type